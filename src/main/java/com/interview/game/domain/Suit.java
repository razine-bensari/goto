package com.interview.game.domain;

/**
 * Represent the suit in the game.
 * Order of the enum needs to be kept. (relied on for sorting)
 */
public enum Suit {
	HEARTS,
	SPADES,
	CLUBS,
	DIAMONDS,
}
